//HOW I SAVED BOOKS in database;

mydb=# CREATE TABLE books (
mydb(# book_id       SERIAL PRIMARY KEY,
mydb(# author        VARCHAR(100) NOT NULL,
mydb(# genre         VARCHAR(100),
mydb(# book_read_date DATE,
mydb(# rating SMALLINT CHECK (rating BETWEEN 1 AND 5),
mydb(# review_text TEXT,
mydb(# title VARCHAR(100));
CREATE TABLE